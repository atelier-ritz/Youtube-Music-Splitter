# Audio Processing Service Environment Variables
# Copy this file to .env and update the values for your environment

# =============================================================================
# BACKEND URL CONFIGURATION
# =============================================================================
# URL of the backend service that will proxy track requests to this service
# This is used to generate track URLs that the frontend can access

# Local Development:
BACKEND_URL=http://localhost:3001

# Network Development (cross-device testing):
# BACKEND_URL=http://192.168.1.100:3001

# Railway Production (using public URLs):
# BACKEND_URL=https://your-backend-service-name.up.railway.app

# Railway Production (using private networking - recommended):
# BACKEND_URL=http://your-backend-service.railway.internal:3001

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port for the audio processing service (Railway sets this automatically)
PORT=5000

# =============================================================================
# DEMUCS MODEL CONFIGURATION
# =============================================================================
# IMPORTANT: DO NOT CHANGE - User requires htdemucs_6s for 6-track separation
# Model: htdemucs_6s (vocals, drums, bass, guitar, piano, other)
# This is configured in the code and should not be overridden

# =============================================================================
# PERFORMANCE TUNING (Optional)
# =============================================================================
# These are set in the code but can be overridden if needed

# Maximum file size for uploads (in bytes)
# MAX_FILE_SIZE=52428800  # 50MB

# Processing timeout multiplier (seconds per minute of audio)
# TIMEOUT_MULTIPLIER=180  # 3 minutes per audio minute

# Memory limit for Demucs process (in GB)
# MEMORY_LIMIT_GB=6

# =============================================================================
# LOGGING AND DEBUG
# =============================================================================
# Set to 'true' to enable debug logging
# DEBUG=false

# Log level for Gunicorn (info, debug, warning, error)
# LOG_LEVEL=info